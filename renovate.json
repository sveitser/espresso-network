{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "matchManagers": ["cargo"],
      "matchPaths": ["Cargo.lock", "sequencer-sqlite/Cargo.lock"],
      "groupName": "Cargo lockfile updates"
    }
  ]
}
